import { createTheme } from '@mui/material/styles'


export const theme_ui = createTheme({
    pallete: {
        primary: {
            main: "#764abc"
        }
    },
    background: {
        default: "#ff43"
    }
}
)
